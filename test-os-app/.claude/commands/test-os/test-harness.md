# /test-os:test-harness

Generate or revise the Hermes harness configuration.

Use this command to configure the app base URL, manifest paths, screenshot capture policy, browser provider, timeouts, and vision analysis.
