# /test-os:run-validation

Run the full Test OS validation suite through Hermes.

Use this command to execute the current manifest against the configured app URL and produce `results.json` plus `report.html`.
